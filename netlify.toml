[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[dev]
  targetPort = 3007

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
